jenkins:
  systemMessage: "T51 Jenkins Server"
  numExecutors: 2

unclassified:
  location:
    url: http://jenkins.${MY_DOMAIN}
  shell:
    shell: "/bin/bash"
  giteaServers:
    servers:
      - displayName: "Gitea-Local"
        serverUrl: "http://gitea:3000"
        # Changed from manageWebhooks to manageHooks
        manageHooks: false

#unclassified:
#  themes:
#    systemTheme: io.jenkins.plugins.darktheme.DarkThemeManagerFactory