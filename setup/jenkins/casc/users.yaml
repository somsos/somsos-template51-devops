jenkins:

  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: ${MY_USER}
          password: ${MY_PASS}

  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false