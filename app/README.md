# DevOps App

The project of DevOps is built using as base bash scripts, and
for more complex tasks as compiling, I use docker.

All the commands are execute at the level of app.

It's required to declare a variable called `export T51_ENV="stage | PRODUCTION"`,
to know how the scripts are going to set up the application.

We just use one branch `main`, to keep it simple.

