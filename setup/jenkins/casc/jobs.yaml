# CAREFUL: Keep sync Pipeline name and working folder in scripts

jobs:
  - script: >
      pipelineJob('Backend-Deploy-v1') {
        
        triggers {
          genericTrigger {
            token("${BACK_NAME}-${SHARED_TOKEN}")
            printPostContent(false)
            printContributedVariables(false)
            causeString('Triggered by Gitea Webhook')
          }
        }

        definition {
          cps {
            script('''
              pipeline {
                agent { label 'built-in' }
                stages {
                stage('1/3: Download') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/Backend-Deploy-v1/1-download.sh
                      \'\'\'
                    }
                  }


                  stage('2/3: Build') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/Backend-Deploy-v1/2-build.sh
                      \'\'\'
                    }
                  }


                  stage('3/3: Deploy') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/Backend-Deploy-v1/3-deploy.sh
                      \'\'\'
                    }
                  }


                }
              }
            '''.stripIndent())
          }
        }
      }


  - script: >
      pipelineJob('Frontend-Deploy-v1') {

        triggers {
          genericTrigger {
            token("${FRONT_NAME}-${SHARED_TOKEN}")
            printPostContent(false)
            printContributedVariables(false)
            causeString('Triggered by Gitea Webhook')
          }
        }

        definition {
          cps {
            script('''
              pipeline {
                agent { label 'built-in' }
                stages {
                stage('1/3: Download') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/Frontend-Deploy-v1/1f-download.sh
                      \'\'\'
                    }
                  }


                  stage('2/3: Build') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/Frontend-Deploy-v1/2f-build.sh
                      \'\'\'
                    }
                  }


                  stage('3/3: Deploy') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/Frontend-Deploy-v1/3f-deploy.sh
                      \'\'\'
                    }
                  }


                }
              }
            '''.stripIndent())
          }
        }
      }


  - script: >
      pipelineJob('Database-Deploy-v1') {
        description('Migrate database to next version.')

        triggers {
          genericTrigger {
            token("${DbMIG_NAME}-${SHARED_TOKEN}")
            printPostContent(false)
            printContributedVariables(false)
            causeString('Triggered by Gitea Webhook')
          }
        }

        definition {
          cps {
            script('''
              pipeline {
                agent { label 'built-in' }
                stages {
                   
                  stage('Guard Rail') {
                    steps {
                      input message: 'Did you already created an database backup?', ok: 'Yes, proceed!'
                    }
                  }
                  
                  stage('1/2: Download') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/db-mig-deploy/1md-download-devops-repo.sh
                      \'\'\'
                    }
                  }


                  stage('2/2: Migrate') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/db-mig-deploy/2md-migrate-deploy.sh
                      \'\'\'
                    }
                  }

                }
              }
            '''.stripIndent())
          }
        }
      }

  - script: |
      pipelineJob('Docker-Control') {
        description('Manage Docker services')
        
        parameters {
          choiceParam('ACTION', ['up -d --wait', 'stop', 'restart'], 'Select action')
          choiceParam('SERVICE', ['db', 'back', 'front', 'gitea', 'reverse-proxy', 'db_backup'], 'Select service')
        }

        definition {
          cps {
            script('''
              pipeline {
                agent { label 'built-in' }
                stages {
                  
                  stage('Guard Rail') {
                    steps {
                      input message: 'Are you sure you want to deploy?', ok: 'Yes, proceed!'
                    }
                  }

                  stage('Download') {
                    steps {
                      sh \'\'\'
                        source $DEVOPS_WORKDIR/docker-control/1dc-download.sh
                      \'\'\'
                    }
                  }

                  stage('Docker Compose Action') {
                    steps {
                      sh "cd $DEVOPS_WORKDIR/docker-control/$BUILD_NUMBER && docker compose ^${params.ACTION} ^${params.SERVICE}"
                    }
                  }

                  stage('Verify Status') {
                    steps {
                      sh \'\'\'
                        cd $DEVOPS_WORKDIR/docker-control/$BUILD_NUMBER
                        docker ps -a --format 'table {{.Names}}\t{{.Status}}\t{{.Size}}'
                      \'\'\'
                    }

                  }
                }
              }
            '''.stripIndent())
          }
        }
      }

  - script: >
      pipelineJob('Database-Rollback-v1') {
        description('Rollback database to previous version.')

        definition {
          cps {
            script('''
              pipeline {
                agent { label 'built-in' }
                stages {
                   
                   stage('Guard Rail') {
                      steps {
                          input message: 'Did you already created an database backup?', ok: 'Yes, proceed!'
                      }
                   }
                  
                  stage('1/2: Download') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/db-mig-rollback/1mr-download-devops-repo.sh
                      \'\'\'
                    }
                  }

                  stage('2/2: Rollback') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/db-mig-rollback/2mr-migrate-rollback.sh
                      \'\'\'
                    }
                  }

                }
              }
            '''.stripIndent())
          }
        }
      }

  - script: >
      pipelineJob('Database-Backup') {
        description('Create a backup of the database.')

        definition {
          cps {
            script('''
              pipeline {
                agent { label 'built-in' }
                stages {
                   
                  stage('Guard Rail') {
                     steps {
                         input message: 'Are you sure you want to continue?', ok: 'Yes, proceed!'
                     }
                  }

                  stage('Backup') {
                    steps {
                      sh \'\'\'
                        source /var/jenkins_home/workspace/Database-Backup/1-backup.sh
                      \'\'\'
                    }
                  }

                }
              }
            '''.stripIndent())
          }
        }
      }

  - script: >
      pipelineJob('Database-Restore') {
        description('Restore database using a backup.')

        parameters {
          stringParam('BACKUP_NAME', '', 'Enter the filename of the database backup to restore (e.g. backup_997.sql)')
        }

        definition {
          cps {
            script('''
              pipeline {
                agent { label 'built-in' }
                stages {
                   
                  stage('Guard Rail') {
                     steps {
                         input message: 'Are you sure you want to continue?', ok: 'Yes, proceed!'
                     }
                  }

                  stage('Restore') {
                    steps {
                      sh "echo 'Restoring from: ^${params.BACKUP_NAME}'"
                      sh "BACKUP_NAME=^${params.BACKUP_NAME} source /var/jenkins_home/workspace/Database-Restore/1-restore.sh"
                    }
                  }

                }
              }
            '''.stripIndent())
          }
        }
      }
