# Manual 2

1. Copy abd peste `.env.example` to create `.env`.
2. Edit the variables with `■` characters, 
   1. in the case of `DOCKER_GID` run the commnad in the comment just right up
      the variable name.
3. Run `docker compose --profile all pull` to download the necesary images from
   docker hub.
